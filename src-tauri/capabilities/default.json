{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Least-privilege capability set for the main window. SQLite access, file open/save dialogs for ledger import/export, reading and writing only the files the user picks in those dialogs, and opening external links. No shell execution, no arbitrary filesystem access, no wallet or device APIs.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "sql:default",
    "sql:allow-load",
    "sql:allow-execute",
    "sql:allow-select",
    "sql:allow-close",
    "dialog:allow-open",
    "dialog:allow-save",
    "fs:allow-read-text-file",
    "fs:allow-write-text-file",
    "opener:allow-open-url"
  ]
}
